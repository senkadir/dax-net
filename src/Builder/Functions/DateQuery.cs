using System.Text;

namespace Builder.Functions
{
    public class DateQuery : QueryBase
    {
        public override StringBuilder Generate(StringBuilder builder)
        {
            return builder;
        }
    }
}
